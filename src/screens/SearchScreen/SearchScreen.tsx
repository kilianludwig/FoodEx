import * as React from 'react';
import * as sc from './SearchScreen.sc';
import {CategoryWrapper} from '../../components/CategoryWrapper/CategoryWrapper';

export const SearchScreen = () => {
  return (
    <sc.SearchScreen>
      <CategoryWrapper />
    </sc.SearchScreen>
  );
};
